// GENERATED BY CFD2 CODEGEN (generic_coupled_update)

// DO NOT EDIT MANUALLY

struct Vector2 {
    x: f32,
    y: f32,
}

struct Constants {
    dt: f32,
    dt_old: f32,
    dtau: f32,
    time: f32,
    viscosity: f32,
    density: f32,
    component: u32,
    alpha_p: f32,
    scheme: u32,
    alpha_u: f32,
    stride_x: u32,
    time_scheme: u32,
    inlet_velocity: f32,
    ramp_time: f32,
}

// Group 0: Fields

@group(0) @binding(0) 
var<storage, read_write> state: array<f32>;

@group(0) @binding(1) 
var<uniform> constants: Constants;

// Group 1: Solution

@group(1) @binding(0) 
var<storage, read> x: array<f32>;

@compute
@workgroup_size(64)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
    let idx = global_id.x;
    let num_cells = arrayLength(&state) / 7u;
    if (idx >= num_cells) {
        return;
    }
    state[idx * 7u + 0u] = x[idx * 4u + 0u];
    state[idx * 7u + 1u] = x[idx * 4u + 1u];
    state[idx * 7u + 2u] = x[idx * 4u + 2u];
    state[idx * 7u + 3u] = x[idx * 4u + 3u];
    state[idx * 7u + 0u] = state[idx * 7u + 0u];
    state[idx * 7u + 4u] = 0.39999998 * (state[idx * 7u + 3u] - 0.5 * (state[idx * 7u + 1u] * state[idx * 7u + 1u] + state[idx * 7u + 2u] * state[idx * 7u + 2u]) / state[idx * 7u + 0u]);
    state[idx * 7u + 5u] = state[idx * 7u + 1u] / state[idx * 7u + 0u];
    state[idx * 7u + 6u] = state[idx * 7u + 2u] / state[idx * 7u + 0u];
}
