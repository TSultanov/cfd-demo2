// This module previously contained `PrimitiveExpr`, which has been unified into
// `cfd2_ir::ast::Expr`. The module is kept empty for now to avoid breaking the
// `pub mod flux;` declaration in lib.rs. It can be fully removed in a follow-up.
