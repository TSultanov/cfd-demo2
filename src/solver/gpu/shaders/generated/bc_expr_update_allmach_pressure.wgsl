// GENERATED BY CFD2 DSL FUSION (bc_expr_update)
// DO NOT EDIT MANUALLY

struct Constants {
    dt: f32,
    dt_old: f32,
    dtau: f32,
    time: f32,
    viscosity: f32,
    density: f32,
    component: u32,
    alpha_p: f32,
    scheme: u32,
    alpha_u: f32,
    stride_x: u32,
    time_scheme: u32,
    inlet_velocity: f32,
    inlet_ramp_time: f32,
}


@group(0) @binding(0) var<storage, read> state: array<f32>;
@group(0) @binding(1) var<uniform> constants: Constants;
@group(0) @binding(2) var<storage, read_write> bc_value: array<f32>;
@group(1) @binding(0) var<storage, read> face_owner: array<u32>;
@group(1) @binding(1) var<storage, read> face_boundary: array<u32>;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
    let idx = global_id.y * constants.stride_x + global_id.x;
    if (idx >= arrayLength(&face_boundary)) { return; }
    let face_boundary_type = face_boundary[idx];
    let owner = face_owner[idx];
    let base = owner * 14u;
    if (face_boundary_type == 1u) {
        bc_value[idx * 3u + 0u] = select(constants.inlet_velocity, constants.inlet_velocity * min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * (126.0 + min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * (-420.0 + min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * (540.0 + min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * (-315.0 + min(max(constants.time / max(constants.inlet_ramp_time, 0.00000000000000000001), 0.0), 1.0) * 70.0)))), constants.inlet_ramp_time > 0.0);
    }
}
