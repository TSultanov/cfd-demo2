// GENERATED BY CFD2 CODEGEN (rhie_chow_dp_update_store_grad_p_fused)

// DO NOT EDIT MANUALLY

struct Constants {
    dt: f32,
    dt_old: f32,
    dtau: f32,
    time: f32,
    viscosity: f32,
    density: f32,
    component: u32,
    alpha_p: f32,
    scheme: u32,
    alpha_u: f32,
    stride_x: u32,
    time_scheme: u32,
}

@group(0) @binding(0) 
var<storage, read_write> state: array<f32>;

@group(0) @binding(1) 
var<uniform> constants: Constants;

@compute
@workgroup_size(64)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
    let idx = global_id.y * constants.stride_x + global_id.x;
    let num_cells = arrayLength(&state) / max(8u, 1u);
    if (idx >= num_cells) {
        return;
    }
    let base = idx * 8u;
    let rho = max(constants.density, 0.000000000001);
    let dt = max(constants.dt, 0.0);
    let d_p = constants.alpha_u * dt / rho;
    state[base + 3u] = d_p;
    state[base + 6u] = state[base + 4u];
    state[base + 7u] = state[base + 5u];
}
