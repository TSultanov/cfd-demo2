use super::coeff_expr::{coeff_cell_expr, coeff_face_expr};
use super::ir::DiscreteSystem;
use super::plan::{momentum_plan, MomentumPlan};
use super::state_access::{state_scalar_expr, state_vec2_expr};
use crate::solver::model::IncompressibleMomentumFields;
use crate::solver::model::backend::StateLayout;
use super::wgsl_ast::{
    AccessMode, Attribute, Block, Function, GlobalVar, Item, Module, Param, Stmt, StructDef,
    StructField, Type,
};
use super::wgsl_dsl as dsl;

pub fn generate_flux_rhie_chow_wgsl(
    system: &DiscreteSystem,
    layout: &StateLayout,
    fields: &IncompressibleMomentumFields,
) -> String {
    let mut module = Module::new();
    module.push(Item::Comment(
        "GENERATED BY CFD2 CODEGEN (flux_rhie_chow)".to_string(),
    ));
    module.push(Item::Comment("DO NOT EDIT MANUALLY".to_string()));
    module.extend(base_items());
    let plan = momentum_plan(system, fields.u.name(), fields.p.name());
    module.push(Item::Function(main_fn(layout, &plan, fields)));
    module.to_wgsl()
}

fn base_items() -> Vec<Item> {
    let mut items = Vec::new();
    items.push(Item::Struct(vector2_struct()));
    items.push(Item::Struct(constants_struct()));
    items.push(Item::Comment("Group 0: Mesh".to_string()));
    items.extend(mesh_bindings());
    items.push(Item::Comment(
        "Group 1: Fields (consolidated state buffers)".to_string(),
    ));
    items.extend(state_bindings());
    items
}

fn vector2_struct() -> StructDef {
    StructDef::new(
        "Vector2",
        vec![
            StructField::new("x", Type::F32),
            StructField::new("y", Type::F32),
        ],
    )
}

fn constants_struct() -> StructDef {
    StructDef::new(
        "Constants",
        vec![
            StructField::new("dt", Type::F32),
            StructField::new("dt_old", Type::F32),
            StructField::new("dtau", Type::F32),
            StructField::new("time", Type::F32),
            StructField::new("viscosity", Type::F32),
            StructField::new("density", Type::F32),
            StructField::new("component", Type::U32),
            StructField::new("alpha_p", Type::F32),
            StructField::new("scheme", Type::U32),
            StructField::new("alpha_u", Type::F32),
            StructField::new("stride_x", Type::U32),
            StructField::new("time_scheme", Type::U32),
            StructField::new("inlet_velocity", Type::F32),
            StructField::new("ramp_time", Type::F32),
        ],
    )
}

fn mesh_bindings() -> Vec<Item> {
    vec![
        storage_var(
            "face_owner",
            Type::array(Type::U32),
            0,
            0,
            AccessMode::Read,
        ),
        storage_var(
            "face_neighbor",
            Type::array(Type::I32),
            0,
            1,
            AccessMode::Read,
        ),
        storage_var(
            "face_areas",
            Type::array(Type::F32),
            0,
            2,
            AccessMode::Read,
        ),
        storage_var(
            "face_normals",
            Type::array(Type::Custom("Vector2".to_string())),
            0,
            3,
            AccessMode::Read,
        ),
        storage_var(
            "cell_centers",
            Type::array(Type::Custom("Vector2".to_string())),
            0,
            4,
            AccessMode::Read,
        ),
        storage_var(
            "cell_vols",
            Type::array(Type::F32),
            0,
            5,
            AccessMode::Read,
        ),
        storage_var(
            "cell_face_offsets",
            Type::array(Type::U32),
            0,
            6,
            AccessMode::Read,
        ),
        storage_var(
            "cell_faces",
            Type::array(Type::U32),
            0,
            7,
            AccessMode::Read,
        ),
        storage_var(
            "cell_face_matrix_indices",
            Type::array(Type::U32),
            0,
            10,
            AccessMode::Read,
        ),
        storage_var(
            "diagonal_indices",
            Type::array(Type::U32),
            0,
            11,
            AccessMode::Read,
        ),
        storage_var(
            "face_boundary",
            Type::array(Type::U32),
            0,
            12,
            AccessMode::Read,
        ),
        storage_var(
            "face_centers",
            Type::array(Type::Custom("Vector2".to_string())),
            0,
            13,
            AccessMode::Read,
        ),
    ]
}

fn state_bindings() -> Vec<Item> {
    vec![
        storage_var(
            "state",
            Type::array(Type::F32),
            1,
            0,
            AccessMode::ReadWrite,
        ),
        storage_var(
            "state_old",
            Type::array(Type::F32),
            1,
            1,
            AccessMode::Read,
        ),
        storage_var(
            "state_old_old",
            Type::array(Type::F32),
            1,
            2,
            AccessMode::Read,
        ),
        storage_var(
            "fluxes",
            Type::array(Type::F32),
            1,
            3,
            AccessMode::ReadWrite,
        ),
        uniform_var("constants", Type::Custom("Constants".to_string()), 1, 4),
    ]
}

fn storage_var(
    name: &str,
    ty: Type,
    group: u32,
    binding: u32,
    access: AccessMode,
) -> Item {
    Item::GlobalVar(GlobalVar::new(
        name,
        ty,
        super::wgsl_ast::StorageClass::Storage,
        Some(access),
        vec![Attribute::Group(group), Attribute::Binding(binding)],
    ))
}

fn uniform_var(name: &str, ty: Type, group: u32, binding: u32) -> Item {
    Item::GlobalVar(GlobalVar::new(
        name,
        ty,
        super::wgsl_ast::StorageClass::Uniform,
        None,
        vec![Attribute::Group(group), Attribute::Binding(binding)],
    ))
}

fn main_fn(
    layout: &StateLayout,
    plan: &MomentumPlan,
    fields: &IncompressibleMomentumFields,
) -> Function {
    let params = vec![Param::new(
        "global_id",
        Type::vec3_u32(),
        vec![Attribute::Builtin("global_invocation_id".to_string())],
    )];
    Function::new(
        "main",
        params,
        None,
        vec![Attribute::Compute, Attribute::WorkgroupSize(64)],
        main_body(layout, plan, fields),
    )
}

fn main_body(
    layout: &StateLayout,
    plan: &MomentumPlan,
    fields: &IncompressibleMomentumFields,
) -> Block {
    let mut stmts = Vec::new();

    stmts.push(dsl::let_(
        "idx",
        "global_id.y * constants.stride_x + global_id.x",
    ));
    stmts.push(dsl::if_block(
        "idx >= arrayLength(&face_areas)",
        dsl::block(vec![Stmt::Return(None)]),
        None,
    ));

    stmts.push(dsl::let_("owner", "face_owner[idx]"));
    stmts.push(dsl::let_("neighbor", "face_neighbor[idx]"));
    stmts.push(dsl::let_("area", "face_areas[idx]"));
    stmts.push(dsl::let_("boundary_type", "face_boundary[idx]"));
    stmts.push(dsl::let_("face_center", "face_centers[idx]"));

    stmts.push(dsl::var("normal", "face_normals[idx]"));
    stmts.push(dsl::let_("c_owner", "cell_centers[owner]"));
    stmts.push(dsl::let_("dx_vec", "face_center.x - c_owner.x"));
    stmts.push(dsl::let_("dy_vec", "face_center.y - c_owner.y"));
    stmts.push(dsl::if_block(
        "dx_vec * normal.x + dy_vec * normal.y < 0.0",
        dsl::block(vec![
            dsl::assign("normal.x", "-normal.x"),
            dsl::assign("normal.y", "-normal.y"),
        ]),
        None,
    ));

    let u_field = fields.u.name();
    let p_field = fields.p.name();
    let d_p_field = fields.d_p.name();
    let grad_p_field = fields.grad_p.name();
    let u_owner_expr = state_vec2_expr(layout, "state", "owner", u_field);
    let dp_owner_expr = state_scalar_expr(layout, "state", "owner", d_p_field);
    let grad_p_owner_expr = state_vec2_expr(layout, "state", "owner", grad_p_field);
    let rho_owner_expr = coeff_cell_expr(
        layout,
        plan.ddt.as_ref().and_then(|op| op.coeff.as_ref()),
        "owner",
        "constants.density",
    );
    let rho_face_expr = coeff_face_expr(
        layout,
        plan.ddt.as_ref().and_then(|op| op.coeff.as_ref()),
        "owner",
        "neigh_idx",
        "lambda",
        "rho_face",
    );

    stmts.push(dsl::var("u_face", &u_owner_expr));
    stmts.push(dsl::var("d_p_face", &dp_owner_expr));
    stmts.push(dsl::var("grad_p_avg", &grad_p_owner_expr));
    stmts.push(dsl::var("rho_face", &rho_owner_expr));

    let interior_block = {
        let u_neigh_expr = state_vec2_expr(layout, "state", "neigh_idx", u_field);
        let dp_neigh_expr = state_scalar_expr(layout, "state", "neigh_idx", d_p_field);
        let grad_p_neigh_expr = state_vec2_expr(layout, "state", "neigh_idx", grad_p_field);
        let p_owner_expr = state_scalar_expr(layout, "state", "owner", p_field);
        let p_neigh_expr = state_scalar_expr(layout, "state", "neigh_idx", p_field);

        dsl::block(vec![
            dsl::let_("neigh_idx", "u32(neighbor)"),
            dsl::let_("u_neigh", &u_neigh_expr),
            dsl::let_("d_p_neigh", &dp_neigh_expr),
            dsl::let_("grad_p_neigh", &grad_p_neigh_expr),
            dsl::let_("c_neigh", "cell_centers[neigh_idx]"),
            dsl::let_(
                "d_own",
                "distance(vec2<f32>(c_owner.x, c_owner.y), vec2<f32>(face_center.x, face_center.y))",
            ),
            dsl::let_(
                "d_neigh",
                "distance(vec2<f32>(c_neigh.x, c_neigh.y), vec2<f32>(face_center.x, face_center.y))",
            ),
            dsl::let_("total_dist", "d_own + d_neigh"),
            dsl::var_typed("lambda", Type::F32, Some("0.5")),
            dsl::if_block(
                "total_dist > 1e-6",
                dsl::block(vec![dsl::assign("lambda", "d_neigh / total_dist")]),
                None,
            ),
            dsl::assign("rho_face", &rho_face_expr),
            dsl::var("u_central", "u_face"),
            dsl::assign("u_central.x", "lambda * u_face.x + (1.0 - lambda) * u_neigh.x"),
            dsl::assign("u_central.y", "lambda * u_face.y + (1.0 - lambda) * u_neigh.y"),
            dsl::assign("u_face", "u_central"),
            dsl::assign("d_p_face", "lambda * d_p_face + (1.0 - lambda) * d_p_neigh"),
            dsl::assign(
                "grad_p_avg.x",
                "lambda * grad_p_avg.x + (1.0 - lambda) * grad_p_neigh.x",
            ),
            dsl::assign(
                "grad_p_avg.y",
                "lambda * grad_p_avg.y + (1.0 - lambda) * grad_p_neigh.y",
            ),
            dsl::let_("dx", "c_neigh.x - c_owner.x"),
            dsl::let_("dy", "c_neigh.y - c_owner.y"),
            dsl::let_("dist_proj", "abs(dx * normal.x + dy * normal.y)"),
            dsl::let_("dist", "max(dist_proj, 1e-6)"),
            dsl::let_("p_own", &p_owner_expr),
            dsl::let_("p_neigh", &p_neigh_expr),
            dsl::let_("grad_p_n", "grad_p_avg.x * normal.x + grad_p_avg.y * normal.y"),
            dsl::let_("p_grad_f", "(p_neigh - p_own) / dist"),
            dsl::let_("rc_term", "d_p_face * area * (grad_p_n - p_grad_f)"),
            dsl::let_("u_n", "u_face.x * normal.x + u_face.y * normal.y"),
            dsl::assign("fluxes[idx]", "rho_face * (u_n * area + rc_term)"),
        ])
    };

    let inlet_block = dsl::block(vec![
        dsl::let_(
            "ramp",
            "smoothstep(0.0, constants.ramp_time, constants.time)",
        ),
        dsl::let_("u_bc", "Vector2(constants.inlet_velocity * ramp, 0.0)"),
        dsl::assign(
            "fluxes[idx]",
            "rho_face * (u_bc.x * normal.x + u_bc.y * normal.y) * area",
        ),
    ]);

    let wall_block = dsl::block(vec![dsl::assign("fluxes[idx]", "0.0")]);

    let outlet_block = dsl::block(vec![
        dsl::let_("u_n", "u_face.x * normal.x + u_face.y * normal.y"),
        dsl::var_typed("rc_term", Type::F32, Some("0.0")),
        dsl::let_(
            "dist_face",
            "distance(vec2<f32>(c_owner.x, c_owner.y), vec2<f32>(face_center.x, face_center.y))",
        ),
        dsl::if_block(
            "dist_face > 1e-6",
            dsl::block(vec![dsl::assign("rc_term", "0.0")]),
            None,
        ),
        dsl::let_("raw_flux", "rho_face * (u_n * area + rc_term)"),
        dsl::assign("fluxes[idx]", "max(0.0, raw_flux)"),
    ]);

    let boundary_block = dsl::block(vec![dsl::if_block(
        "boundary_type == 1u",
        inlet_block,
        Some(dsl::block(vec![dsl::if_block(
            "boundary_type == 3u",
            wall_block,
            Some(dsl::block(vec![dsl::if_block(
                "boundary_type == 2u",
                outlet_block,
                Some(dsl::block(vec![dsl::assign("fluxes[idx]", "0.0")])),
            )])),
        )])),
    )]);

    stmts.push(dsl::if_block(
        "neighbor != -1",
        interior_block,
        Some(boundary_block),
    ));

    Block::new(stmts)
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::solver::codegen::lower_system;
    use crate::solver::model::incompressible_momentum_model;
    use crate::solver::model::backend::SchemeRegistry;
    use crate::solver::scheme::Scheme;

    #[test]
    fn flux_rhie_chow_codegen_emits_state_arrays() {
        let model = incompressible_momentum_model();
        let schemes = SchemeRegistry::new(Scheme::Upwind);
        let discrete = lower_system(&model.system, &schemes);
        let fields = model.fields.incompressible().expect("incompressible fields");
        let wgsl = generate_flux_rhie_chow_wgsl(&discrete, &model.state_layout, fields);
        assert!(wgsl.contains("state: array<f32>"));
        assert!(wgsl.contains("face_owner"));
        assert!(wgsl.contains("smoothstep"));
    }
}
