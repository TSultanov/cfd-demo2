// GENERATED BY CFD2 DSL FUSION (rhie_chow/correct_velocity_delta)
// DO NOT EDIT MANUALLY

struct Constants {
    dt: f32,
    dt_old: f32,
    dtau: f32,
    time: f32,
    viscosity: f32,
    density: f32,
    component: u32,
    alpha_p: f32,
    scheme: u32,
    alpha_u: f32,
    stride_x: u32,
    time_scheme: u32,
}


@group(0) @binding(0) var<storage, read_write> state: array<f32>;
@group(0) @binding(1) var<uniform> constants: Constants;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
    let idx = global_id.y * constants.stride_x + global_id.x;
    if (idx >= (arrayLength(&state) / 8u)) { return; }
    let base = idx * 8u;
    let d_p = state[base + 3u];
    let grad_px = state[base + 4u];
    let grad_py = state[base + 5u];
    let grad_old_x = state[base + 6u];
    let grad_old_y = state[base + 7u];
    let corr_x = d_p * (grad_px - grad_old_x);
    let corr_y = d_p * (grad_py - grad_old_y);
    state[base + 0u] = state[base + 0u] - corr_x;
    state[base + 1u] = state[base + 1u] - corr_y;
}
