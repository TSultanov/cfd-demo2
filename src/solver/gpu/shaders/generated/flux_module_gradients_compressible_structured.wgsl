// GENERATED BY CFD2 DSL FUSION (flux_module_gradients)
// DO NOT EDIT MANUALLY

struct Vector2 {
    x: f32,
    y: f32,
}

struct StructuredGrid {
    nx: u32,
    ny: u32,
    dx: f32,
    dy: f32,
}

struct Constants {
    dt: f32,
    dt_old: f32,
    dtau: f32,
    time: f32,
    viscosity: f32,
    density: f32,
    component: u32,
    alpha_p: f32,
    scheme: u32,
    alpha_u: f32,
    stride_x: u32,
    time_scheme: u32,
    inlet_velocity: f32,
    inlet_ramp_time: f32,
}


@group(0) @binding(0) var<uniform> grid: StructuredGrid;
@group(1) @binding(0) var<storage, read_write> state: array<f32>;
@group(1) @binding(1) var<uniform> constants: Constants;
@group(2) @binding(0) var<storage, read> bc_kind: array<u32>;
@group(2) @binding(1) var<storage, read> bc_value: array<f32>;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
    let idx = global_id.y * constants.stride_x + global_id.x;
    if (idx >= grid.nx * grid.ny) { return; }
    let sfd_gi = idx % grid.nx;
    let sfd_gj = idx / grid.nx;
    let sfd_cx = (f32(sfd_gi) + 0.5) * grid.dx;
    let sfd_cy = (f32(sfd_gj) + 0.5) * grid.dy;
    let sfd_vol = grid.dx * grid.dy;
    let cell_center_vec: vec2<f32> = vec2<f32>(sfd_cx, sfd_cy);
    let vol = sfd_vol;
    var grad_acc_rho_u_x: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_rho_u_y: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_rho: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_rho_e: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_T: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_u_x: vec2<f32> = vec2<f32>(0.0, 0.0);
    var grad_acc_u_y: vec2<f32> = vec2<f32>(0.0, 0.0);
    for (var k = 0u; k < 4u; k++) {
        let sfd_axis_is_x = k >= 1u && k <= 2u;
        let sfd_sign = select(-1.0, 1.0, k >= 2u);
        let sfd_normal_x = select(0.0, sfd_sign, sfd_axis_is_x);
        let sfd_normal_y = select(sfd_sign, 0.0, sfd_axis_is_x);
        let sfd_area = select(grid.dx, grid.dy, sfd_axis_is_x);
        let sfd_spacing = select(grid.dy, grid.dx, sfd_axis_is_x);
        let sfd_half = 0.5 * sfd_spacing;
        let sfd_coord = select(sfd_gj, sfd_gi, sfd_axis_is_x);
        let sfd_ext = select(grid.ny, grid.nx, sfd_axis_is_x);
        let sfd_is_boundary = select(sfd_coord == sfd_ext - 1u, sfd_coord == 0u, k < 2u);
        let sfd_off = select(grid.nx, 1u, sfd_axis_is_x);
        let sfd_neighbor = select(idx - sfd_off, idx + sfd_off, k >= 2u);
        let sfd_other_idx = select(sfd_neighbor, idx, sfd_is_boundary);
        let sfd_face_cx = sfd_cx + sfd_half * sfd_normal_x;
        let sfd_face_cy = sfd_cy + sfd_half * sfd_normal_y;
        let sfd_mult = select(sfd_spacing, sfd_half, sfd_is_boundary);
        let sfd_other_cx = sfd_cx + sfd_mult * sfd_normal_x;
        let sfd_other_cy = sfd_cy + sfd_mult * sfd_normal_y;
        let sfd_band_rank = select(k + 1u, k, k < 2u);
        let sfd_face_id = idx * 4u + k;
        let face_idx = sfd_face_id;
        let is_boundary = sfd_is_boundary;
        let boundary_type = 0u;
        let area = sfd_area;
        let face_center_vec: vec2<f32> = vec2<f32>(sfd_face_cx, sfd_face_cy);
        let normal_vec: vec2<f32> = vec2<f32>(sfd_normal_x, sfd_normal_y);
        let other_idx = sfd_other_idx;
        let other_center_vec: vec2<f32> = vec2<f32>(sfd_other_cx, sfd_other_cy);
        let d_own = abs(dot(face_center_vec - cell_center_vec, normal_vec));
        let d_neigh = abs(dot(other_center_vec - face_center_vec, normal_vec));
        let total_dist = d_own + d_neigh;
        var lambda: f32 = 0.5;
        if (total_dist > 0.000001) {
            lambda = d_neigh / total_dist;
        }
        let lambda_other = 1.0 - lambda;
        grad_acc_rho_u_x += normal_vec * (state[idx * 23u + 1u] * lambda + select(select(state[other_idx * 23u + 1u], select(select(state[idx * 23u + 1u], bc_value[face_idx * 8u + 1u], bc_kind[face_idx * 8u + 1u] == 1u), state[idx * 23u + 1u] + bc_value[face_idx * 8u + 1u] * d_own, bc_kind[face_idx * 8u + 1u] == 2u), is_boundary), state[idx * 23u + 1u] - (state[idx * 23u + 1u] * normal_vec.x + state[idx * 23u + 2u] * normal_vec.y) * normal_vec.x, is_boundary && boundary_type == 4u) * lambda_other) * area;
        grad_acc_rho_u_y += normal_vec * (state[idx * 23u + 2u] * lambda + select(select(state[other_idx * 23u + 2u], select(select(state[idx * 23u + 2u], bc_value[face_idx * 8u + 2u], bc_kind[face_idx * 8u + 2u] == 1u), state[idx * 23u + 2u] + bc_value[face_idx * 8u + 2u] * d_own, bc_kind[face_idx * 8u + 2u] == 2u), is_boundary), state[idx * 23u + 2u] - (state[idx * 23u + 1u] * normal_vec.x + state[idx * 23u + 2u] * normal_vec.y) * normal_vec.y, is_boundary && boundary_type == 4u) * lambda_other) * area;
        grad_acc_rho += normal_vec * (state[idx * 23u + 0u] * lambda + select(state[other_idx * 23u + 0u], select(select(state[idx * 23u + 0u], bc_value[face_idx * 8u + 0u], bc_kind[face_idx * 8u + 0u] == 1u), state[idx * 23u + 0u] + bc_value[face_idx * 8u + 0u] * d_own, bc_kind[face_idx * 8u + 0u] == 2u), is_boundary) * lambda_other) * area;
        grad_acc_rho_e += normal_vec * (state[idx * 23u + 7u] * lambda + select(state[other_idx * 23u + 7u], select(select(state[idx * 23u + 7u], bc_value[face_idx * 8u + 3u], bc_kind[face_idx * 8u + 3u] == 1u), state[idx * 23u + 7u] + bc_value[face_idx * 8u + 3u] * d_own, bc_kind[face_idx * 8u + 3u] == 2u), is_boundary) * lambda_other) * area;
        grad_acc_T += normal_vec * (state[idx * 23u + 9u] * lambda + select(state[other_idx * 23u + 9u], select(select(state[idx * 23u + 9u], bc_value[face_idx * 8u + 7u], bc_kind[face_idx * 8u + 7u] == 1u), state[idx * 23u + 9u] + bc_value[face_idx * 8u + 7u] * d_own, bc_kind[face_idx * 8u + 7u] == 2u), is_boundary) * lambda_other) * area;
        grad_acc_u_x += normal_vec * (state[idx * 23u + 10u] * lambda + select(select(state[other_idx * 23u + 10u], select(select(state[idx * 23u + 10u], bc_value[face_idx * 8u + 4u], bc_kind[face_idx * 8u + 4u] == 1u), state[idx * 23u + 10u] + bc_value[face_idx * 8u + 4u] * d_own, bc_kind[face_idx * 8u + 4u] == 2u), is_boundary), state[idx * 23u + 10u] - (state[idx * 23u + 10u] * normal_vec.x + state[idx * 23u + 11u] * normal_vec.y) * normal_vec.x, is_boundary && boundary_type == 4u) * lambda_other) * area;
        grad_acc_u_y += normal_vec * (state[idx * 23u + 11u] * lambda + select(select(state[other_idx * 23u + 11u], select(select(state[idx * 23u + 11u], bc_value[face_idx * 8u + 5u], bc_kind[face_idx * 8u + 5u] == 1u), state[idx * 23u + 11u] + bc_value[face_idx * 8u + 5u] * d_own, bc_kind[face_idx * 8u + 5u] == 2u), is_boundary), state[idx * 23u + 11u] - (state[idx * 23u + 10u] * normal_vec.x + state[idx * 23u + 11u] * normal_vec.y) * normal_vec.y, is_boundary && boundary_type == 4u) * lambda_other) * area;
    }
    let grad_out_rho_u_x: vec2<f32> = grad_acc_rho_u_x * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 3u] = grad_out_rho_u_x.x;
    state[idx * 23u + 4u] = grad_out_rho_u_x.y;
    let grad_out_rho_u_y: vec2<f32> = grad_acc_rho_u_y * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 5u] = grad_out_rho_u_y.x;
    state[idx * 23u + 6u] = grad_out_rho_u_y.y;
    let grad_out_rho: vec2<f32> = grad_acc_rho * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 12u] = grad_out_rho.x;
    state[idx * 23u + 13u] = grad_out_rho.y;
    let grad_out_rho_e: vec2<f32> = grad_acc_rho_e * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 14u] = grad_out_rho_e.x;
    state[idx * 23u + 15u] = grad_out_rho_e.y;
    let grad_out_T: vec2<f32> = grad_acc_T * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 16u] = grad_out_T.x;
    state[idx * 23u + 17u] = grad_out_T.y;
    let grad_out_u_x: vec2<f32> = grad_acc_u_x * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 18u] = grad_out_u_x.x;
    state[idx * 23u + 19u] = grad_out_u_x.y;
    let grad_out_u_y: vec2<f32> = grad_acc_u_y * 1.0 / max(vol, 0.000000000001);
    state[idx * 23u + 20u] = grad_out_u_y.x;
    state[idx * 23u + 21u] = grad_out_u_y.y;
}
